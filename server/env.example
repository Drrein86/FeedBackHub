# Backend Environment Variables

# Database Configuration
# For local development with PostgreSQL:
# DATABASE_URL="postgresql://username:password@localhost:5432/feedbackhub"
# 
# For Vercel deployment with Supabase (recommended):
# Get your connection string from: https://supabase.com/dashboard/project/_/settings/database
# Example: DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"
#
# For Vercel Postgres:
# DATABASE_URL will be automatically set by Vercel when you add Postgres storage
DATABASE_URL="postgresql://username:password@localhost:5432/feedbackhub"

# JWT Secret for authentication
# Generate a strong random secret key (at least 32 characters)
# You can generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production-use-random-32-chars"

# Server Configuration
PORT=3001
NODE_ENV=development

# Frontend URL (for CORS configuration in production)
# For Vercel deployment, set this to your frontend URL
FRONTEND_URL="https://your-app-name.vercel.app"

# For production deployment on Vercel:
# 1. Add a PostgreSQL database (Supabase recommended or Vercel Postgres)
# 2. Set these environment variables in Vercel dashboard:
#    - DATABASE_URL: Your database connection string (automatically set if using Vercel Postgres)
#    - JWT_SECRET: A strong, unique secret key (generate with crypto)
#    - NODE_ENV: production
#    - FRONTEND_URL: Your frontend Vercel URL
